# Boolean operators

# && (AND)

true && false#& ambersand

# || (OR)

true || false #verticle bar

# ! (NOT)

!true

# Why && / || instead of & / |

# && 
# & for bitwise and
#single & | bit operators

# Numerical comparisons

# Equality: == and !=

5 != 5.0

# Less than: < and <= \ge

5 <= 5
5 ≤ 6# \ge

# Greater than: > and >= \ge

10 ≥ 2

(10 < 2) && (5 > 2) || (5 > 2)
!((10 < 2) && (5 > 2) || (5 > 2))